import 'babel-polyfill';
import regeneratorRuntime from 'babel-regenerator-runtime';
import express from 'express';

let app = express();

import { MyTokens,something } from './tokens.js';


// MyTokens(100);
something();












// let port = 3030 || process.env.PORT;
//
// app.listen(port,()=>{
//   console.log('listening on port '+port+' for INFURA');
// });
